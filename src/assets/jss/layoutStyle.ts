import { createStyles } from '@material-ui/core/styles'

const layoutStyle = () =>
    createStyles({
        main: {
            flexGrow: 1,
            flexShrink: 0,
        },
    })

export default layoutStyle
