import { createStyles } from '@material-ui/core/styles'

const aboutStyle = () =>
    createStyles({
        container: {
            padding: '100px 0',
        },
        aboutTitle: {
            fontWeight: 600,
        },
    })

export default aboutStyle
